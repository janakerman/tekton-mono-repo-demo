apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: echo-val-task
spec:
  params:
    - name: service
      type: string
  steps:
    - name: echo
      image: alpine
      script: |
        echo "Echoing for $SERVICE"
      env:
        - name: SERVICE
          value: $(params.service)
